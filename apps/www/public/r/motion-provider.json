{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "motion-provider",
  "type": "registry:lib",
  "title": "Motion Provider",
  "description": "Motion provider for the project",
  "dependencies": [
    "motion"
  ],
  "files": [
    {
      "path": "registry/default/lib/motion-provider.tsx",
      "content": "'use client'\n\nimport { LazyMotion, domAnimation } from 'motion/react'\n\nexport default function MotionProvider({\n    children,\n}: {\n    children: React.ReactNode\n}) {\n    return <LazyMotion features={domAnimation}>{children}</LazyMotion>\n}\n",
      "type": "registry:file",
      "target": "lib/motion-provider.tsx"
    }
  ],
  "docs": "Wrap your app with this provider to enable motion features."
}